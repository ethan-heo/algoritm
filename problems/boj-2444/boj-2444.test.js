import { describe, it, expect } from 'vitest';
const { solution } = require('./boj-2444.js');

describe('백준 2444 - 별찍기 - 7', () => {
	it('예제 입력 1', () => {
		const input = `8`;
		solution(input);
	});
});
