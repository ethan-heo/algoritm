{
	"name": "algorithm-practice",
	"version": "1.0.0",
	"description": "Algorithm practice workspace with JavaScript and Vitest",
	"scripts": {
		"test": "vitest",
		"test:ui": "vitest --ui",
		"test:run": "vitest run",
		"format": "prettier --write .",
		"format:check": "prettier --check .",
		"new": "node scripts/create-problem.js"
	},
	"keywords": ["algorithm", "javascript", "vitest"],
	"author": "",
	"license": "MIT",
	"devDependencies": {
		"prettier": "^3.3.0",
		"vitest": "^2.0.0"
	}
}
