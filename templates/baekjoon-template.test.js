import { describe, it, expect } from 'vitest';
const { solution } = require('./baekjoon-template.js');

describe('백준 XXXXX - 문제 제목', () => {
	it('예제 입력 1', () => {
		const input = ``;
		const expected = ``;

		expect(solution(input)).toBe(expected);
	});

	it('예제 입력 2', () => {
		const input = ``;
		const expected = ``;

		expect(solution(input)).toBe(expected);
	});
});
